============
Installation
============

At the command line::

    $ easy_install django-auth-backend-pam

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-auth-backend-pam
    $ pip install django-auth-backend-pam
